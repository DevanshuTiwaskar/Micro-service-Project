events {}

http {
  client_max_body_size 50M;
  # Include default MIME types
  types {
    text/html                             html htm shtml;
    text/css                              css;
    text/xml                              xml;
    image/gif                             gif;
    image/jpeg                            jpeg jpg;
    image/png                             png;
    image/svg+xml                         svg svgz;
    image/webp                            webp;
    image/x-icon                          ico;
    application/javascript                js;
    application/json                      json;
    application/wasm                      wasm;
    font/woff                             woff;
    font/woff2                            woff2;
  }
  
  default_type application/octet-stream;
  upstream auth_service {
    server auth:4000;
  }

  upstream music_service {
    server music:4002;
  }

  upstream notification_service {
    server notification:3001;
  }

  upstream frontend_service {
    server frontend:80;
  }

 

  server {
    listen 80;

    # -------- AUTH --------
    location /api/auth/ {
      proxy_pass http://auth_service;
      proxy_set_header Host $host;
      proxy_set_header X-Real-IP $remote_addr;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    # -------- MUSIC --------
    location /api/music/ {
      proxy_pass http://music_service;
      proxy_set_header Host $host;
      proxy_set_header X-Real-IP $remote_addr;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    # -------- NOTIFICATION --------
    location /api/notification/ {
      proxy_pass http://notification_service;
      proxy_set_header Host $host;
      proxy_set_header X-Real-IP $remote_addr;
    }

    # -------- FRONTEND (catch-all for SPA routing) --------
    location / {
      proxy_pass http://frontend_service;
      proxy_set_header Host $host;
      proxy_set_header X-Real-IP $remote_addr;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
  }
}


# events {}

# http {
#   # 👇 THIS is the magic line for Kubernetes D      NS
#   resolver kube-dns.kube-system.svc.cluster.local valid=10s;

#   server {
#     listen 80;

#     # -------- AUTH --------
#     location /api/auth/ {
#       set $auth_service auth;
#       proxy_pass http://$auth_service:3000;
#       proxy_set_header Host $host;
#       proxy_set_header X-Real-IP $remote_addr;
#       proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#     }

#     # -------- MUSIC --------
#     location /api/music/ {
#       set $music_service music;
#       proxy_pass http://$music_service:3002;
#       proxy_set_header Host $host;
#       proxy_set_header X-Real-IP $remote_addr;
#       proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#     }

#     # -------- NOTIFICATION --------
#     location /api/notification/ {
#       set $notification_service notification;
#       proxy_pass http://$notification_service:3001;
#       proxy_set_header Host $host;
#       proxy_set_header X-Real-IP $remote_addr;
#     }
#   }
# }
